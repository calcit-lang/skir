{
  "name": "skir",
  "version": "0.0.10",
  "main": "index.js",
  "repository": "git@github.com:mvc-works/skir.git",
  "author": "jiyinyiyong",
  "scripts": {
    "bundle-main": "yarn esbuild main.js --bundle --platform=node --outfile=dist/bundle.js"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.12.26"
  },
  "dependencies": {
    "@calcit/procs": "^0.4.16"
  }
}

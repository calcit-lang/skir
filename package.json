{
  "name": "skir",
  "version": "0.0.13",
  "main": "index.js",
  "repository": "git@github.com:mvc-works/skir.git",
  "author": "jiyinyiyong",
  "scripts": {
    "bundle-main": "yarn esbuild main.mjs --bundle --platform=node --outfile=dist/bundle.js"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.16.4"
  },
  "dependencies": {
    "@calcit/procs": "^0.6.16"
  }
}

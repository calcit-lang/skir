# skir 0.0.23

- Upgrade Calcit and `@calcit/procs` to 0.13.77.
- Align `respo-router.calcit` with the published 0.8.13 release.
- Add strict dependency, toolchain, snapshot, dynamic-method, and quality gates to CI.
- Add static string contracts to router helpers and migrate deprecated `tag-match` to `match`.
- Adapt route matching to the current `Option` contract of `get-in`, `nth`, and `first`.
- Baseline the two reviewed dynamic method calls at the Node.js response-event FFI boundary.
